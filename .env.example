# ============================================================================
# OpenID Connect (OIDC) Configuration
# ============================================================================
# Provider-agnostic OIDC configuration supporting Keycloak, Entra ID, Auth0, etc.

# OIDC Issuer (optional, used for token validation)
VITE_OIDC_ISSUER=

# OIDC Endpoints
VITE_OIDC_AUTHORIZATION_ENDPOINT=
VITE_OIDC_TOKEN_ENDPOINT=
VITE_OIDC_USERINFO_ENDPOINT=
VITE_OIDC_END_SESSION_ENDPOINT=
VITE_OIDC_REVOCATION_ENDPOINT=

# Client Configuration
VITE_OIDC_CLIENT_ID=
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# Scopes (space-separated)
VITE_OIDC_SCOPE=openid profile email

# Claims Configuration
VITE_OIDC_ROLES_CLAIM=roles
VITE_OIDC_PERMISSIONS_CLAIM=permissions

# ============================================================================
# DEPRECATED: OAuth 2.0 Configuration (Legacy)
# ============================================================================
# These variables are deprecated and maintained for backward compatibility only.
# Please migrate to VITE_OIDC_* variables above.
# Support for VITE_OAUTH_* variables will be removed in a future version.

# VITE_OAUTH_AUTHORIZATION_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/authorize
# VITE_OAUTH_TOKEN_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# VITE_OAUTH_CLIENT_ID=your-client-id
# VITE_OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback
# VITE_OAUTH_POST_LOGOUT_URI=http://localhost:5173
# VITE_OAUTH_SCOPE=openid profile email


# ============================================================================
# Provider-Specific Configuration Examples
# ============================================================================

# ----------------------------------------------------------------------------
# Keycloak Configuration Example
# ----------------------------------------------------------------------------
# VITE_OIDC_ISSUER=https://keycloak.example.com/realms/myrealm
# VITE_OIDC_AUTHORIZATION_ENDPOINT=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/auth
# VITE_OIDC_TOKEN_ENDPOINT=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/token
# VITE_OIDC_USERINFO_ENDPOINT=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/userinfo
# VITE_OIDC_END_SESSION_ENDPOINT=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/logout
# VITE_OIDC_REVOCATION_ENDPOINT=https://keycloak.example.com/realms/myrealm/protocol/openid-connect/revoke
# VITE_OIDC_CLIENT_ID=your-keycloak-client-id
# VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
# VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173
# VITE_OIDC_SCOPE=openid profile email
# VITE_OIDC_ROLES_CLAIM=realm_access.roles
# VITE_OIDC_PERMISSIONS_CLAIM=permissions
#
# Note: Keycloak stores realm roles in "realm_access.roles" by default.
# Client roles are in "resource_access.{client-id}.roles".

# ----------------------------------------------------------------------------
# Entra ID (Azure AD) Configuration Example
# ----------------------------------------------------------------------------
# VITE_OIDC_ISSUER=https://login.microsoftonline.com/{tenant-id}/v2.0
# VITE_OIDC_AUTHORIZATION_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/authorize
# VITE_OIDC_TOKEN_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# VITE_OIDC_USERINFO_ENDPOINT=https://graph.microsoft.com/oidc/userinfo
# VITE_OIDC_END_SESSION_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/logout
# VITE_OIDC_REVOCATION_ENDPOINT=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/revoke
# VITE_OIDC_CLIENT_ID=your-entra-client-id
# VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
# VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173
# VITE_OIDC_SCOPE=openid profile email
# VITE_OIDC_ROLES_CLAIM=roles
# VITE_OIDC_PERMISSIONS_CLAIM=permissions
#
# Note: Entra ID stores app roles in the "roles" claim by default.
# You must configure app roles in your Azure AD app registration.
# Replace {tenant-id} with your Azure AD tenant ID or domain name.

# ----------------------------------------------------------------------------
# Auth0 Configuration Example
# ----------------------------------------------------------------------------
# VITE_OIDC_ISSUER=https://{domain}.auth0.com/
# VITE_OIDC_AUTHORIZATION_ENDPOINT=https://{domain}.auth0.com/authorize
# VITE_OIDC_TOKEN_ENDPOINT=https://{domain}.auth0.com/oauth/token
# VITE_OIDC_USERINFO_ENDPOINT=https://{domain}.auth0.com/userinfo
# VITE_OIDC_END_SESSION_ENDPOINT=https://{domain}.auth0.com/v2/logout
# VITE_OIDC_REVOCATION_ENDPOINT=https://{domain}.auth0.com/oauth/revoke
# VITE_OIDC_CLIENT_ID=your-auth0-client-id
# VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
# VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173
# VITE_OIDC_SCOPE=openid profile email
# VITE_OIDC_ROLES_CLAIM=https://myapp.com/roles
# VITE_OIDC_PERMISSIONS_CLAIM=https://myapp.com/permissions
#
# Note: Auth0 requires namespaced custom claims (e.g., https://myapp.com/roles).
# Configure roles using Auth0 Rules or Actions to add custom claims to tokens.
# Replace {domain} with your Auth0 tenant domain (e.g., myapp.us.auth0.com).
